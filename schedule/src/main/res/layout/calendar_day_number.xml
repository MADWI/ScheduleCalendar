<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="@layout/calendar_week_numbers">

    <TextView
        android:layout_width="0dp"
        android:layout_height="@dimen/calendar_day_number_height"
        android:layout_weight="1"
        android:drawablePadding="-10dp"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="@dimen/calendar_day_number_text_size"
        tools:background="@color/scheduleColorSecondary"
        tools:drawableBottom="@drawable/calendar_day_lessons_indicator"
        tools:layout_width="40dp"
        tools:text="20" />

</merge>
